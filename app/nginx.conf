server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # go_router client-side routes — serve index.html for unknown paths.
    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }

    # Flutter web bootstrap, entrypoint scripts, and the runtime backend-URL
    # config must revalidate on every reload — config.json changes per
    # container restart (see docker-entrypoint.sh), so a cached copy would
    # keep pointing a browser at the wrong environment.
    location ~* (index\.html|flutter_bootstrap\.js|flutter_service_worker\.js|main\.dart\.js|config\.json)$ {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        add_header Pragma "no-cache";
        add_header Expires 0;
    }

    # Static media and fonts can be cached short-term
    location ~* \.(css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 7d;
        add_header Cache-Control "public, no-transform";
    }

    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;
}
