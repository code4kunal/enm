SEVERITY: high
PERSONA:  QA_MGR / any inspector @ MBMT
ORACLE:   design/HANDOFF.md — an inspection is a checklist sweep with its own
          form and record; CLAUDE.md — D.I and the ten-day service "have their
          own checklist, their own form and their own record";
          PENDING.md — "Inspection checklists live: D.I 14/14/12 checks by bus
          model, ten-day 57 each"
REPRO:
  1. GET /sites/MBMT/checklists — note an item id on the D.I / "12M AC" template.
  2. GET /sites/MBMT/vehicles — pick a bus whose checklist_variant is "12M AC".
  3. POST /sites/MBMT/inspections with that vehicle, that work type, a date with
     no inspection yet, and results [{item_id, result: "ok"}].
EXPECTED: The answer is recorded against the inspection.
ACTUAL:   400 VALIDATION_ERROR
          {"results": "unknown item"} — "That checklist line is not on this
          site's checklist"

          Every template, every variant, every bus:

          work type         variant      bus           items  result
          D.I               12M AC       MH04LY6796    14     400
          D.I               12M Non-AC   MH04LY9683    12     400
          D.I               9M           MH04LQ5736    14     400
          10 DAYS SERVICE   12M AC       MH04LY6796    57     400
          10 DAYS SERVICE   12M Non-AC   MH04LY9683    57     400
          10 DAYS SERVICE   9M           MH04LQ5736    57     400

          The same request with `results: []` returns 201. An inspection can be
          filed only if it records nothing.

IMPACT:   In MBMT's own data, across every sweep ever recorded:

          work type         inspections   recorded answers
          10 DAYS SERVICE   49            0
          D.I               34            0
          P.M                7            0

          Ninety inspections. Not one checklist line has ever been saved. The
          system records that a sweep happened and never what was checked,
          which is the entire content of the record.

MECHANISM: `create_inspection` calls
          `ensure_template(session, site_code, work_type)` and omits the
          vehicle's `checklist_variant`. `ensure_template` defaults
          `variant=None`, so it resolves the site's variant-less template —
          which holds zero items, having been created as a placeholder — and
          every submitted item is therefore unknown. The required-line check
          passes for the same reason: an empty template requires nothing.

          Found black-box first: the systematic 400s and the 201-on-empty are
          observable without reading any of this.

ALSO EXPLAINS 0005. The tyre-pressure and bus-washing charts read checklist
answers. Nominating a `chart_key` line would not have filled them, because no
answer can be stored against any line.

FIX:      Pass the bus's variant, as the seeding script already does:
              template = await ensure_template(
                  session, site_code, work_type, variant=vehicle.checklist_variant
              )
          One argument. The 90 existing inspections cannot be recovered — the
          answers were never sent to a store that could hold them.

TEST:     qa/api/test_inspections.py::test_an_inspection_records_the_answers_given
