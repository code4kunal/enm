SEVERITY: medium
PERSONA:  QA_EXEC (executive) @ QASITE
ORACLE:   CLAUDE.md:29 ("Roles, most to least privileged: admin -> manager ->
          supervisor -> executive"); design/HANDOFF.md:93 ("role-based access");
          backend/README.md "Permissions"
REPRO:
  1. Log in as an executive with site_access to QASITE.
  2. POST /entries for work_done, coolant, driver_complaint or breakdown.
EXPECTED: Unclear, and that is the finding. An executive is named the least
          privileged role, yet has write access identical to a manager's.
ACTUAL:   201 Created on all four live registers.

          role        work_done  coolant  driver_complaint  breakdown
          manager     201        201      201               201
          supervisor  201        201      201               201
          executive   201        201      201               201

TEST:     qa/api/test_permissions.py::test_an_executive_may_not_file_work

RULING NEEDED. Nothing in the oracle says an executive may not create an entry.
The README restricts entry *edit* ("creator, or any supervisor/manager") and
`/admin/*` ("manager only"), and HANDOFF gates only the Admin tab. So this may
be intended.

But if the ladder does not constrain entry creation, three of the four roles
are indistinguishable for the app's central action, and the ladder is
decoration. Someone should decide which, and the answer should be written down.

The test is xfail, not adjusted to match the code. Changing the assertion to
`201` would encode the current behaviour as the requirement, which is exactly
the developer reflex this suite exists to avoid.
