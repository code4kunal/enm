SEVERITY: medium
PERSONA:  QA_MGR (manager) @ QASITE
ORACLE:   CLAUDE.md — dates are `yyyy-MM-dd` and "sort lexically, which is what
          the period filters rely on"; design/HANDOFF.md section 5 (Today /
          Last 7 days / This month / Custom range)
REPRO:
  1. POST /entries with "date": "2262-01-01" (or "0001-01-01").
EXPECTED: A maintenance record belongs to a date the fleet could plausibly have
          been operating. A depot commissioned in the 2020s cannot have a bus
          serviced in the year 1.
ACTUAL:   201 Created. Every date tested was accepted.

          date          result
          2099-01-01    201
          2262-01-01    201
          1900-01-01    201
          0001-01-01    201

TEST:     qa/api/test_field_validation.py::
          test_a_date_far_outside_the_fleet_lifetime_is_refused

RULING NEEDED on where the bounds sit; the behaviour is not in doubt.

This matters because of how the entry is then lost. The period chips are
Today / Last 7 days / This month, so a record typed as 2062 instead of 2026
appears in no filter a supervisor uses, contributes to no DMR anybody reads,
and is not visibly missing either — the register simply has one fewer line
than the fitter remembers writing. Verified: 42 entries on the QA site,
period=month returns 37.

STATUS:   CLOSED 2026-08-19. `assert_date_is_plausible` bounds an entry to
          the site's `commissioned_on` (or 2000-01-01 where the site does not
          record one) through to tomorrow. Tomorrow rather than today because a
          night shift crosses midnight and a device can be an hour off.

          Applied on create and on edit — the date is editable, so a guard on
          the way in only is no guard at all. Backfilling a month still works,
          which is what the MBMT import does; the bound exists to catch a typed
          year, not to police history.
