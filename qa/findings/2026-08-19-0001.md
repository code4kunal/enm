SEVERITY: low
PERSONA:  any, pre-login
ORACLE:   design/HANDOFF.md section 1 ("Screens / Views — 1. Login")
REPRO:
  1. Open the client at the login screen.
  2. Look at the card.
EXPECTED: HANDOFF specifies a "Sign in with Microsoft" button — white, 1.5px
          #D6D9D2 border, MS 4-square logo, 16px/600 — above a divider reading
          "OR SIGN IN WITH USER ID".
ACTUAL:   No Microsoft button is rendered. The divider reads "SIGN IN WITH USER
          ID", without the leading "OR". Only the User ID path exists.
TEST:     app/integration_test/smoke_boots_test.dart::
          the login screen offers the Microsoft sign-in HANDOFF promises

NOTE:     Consistent with PENDING.md section 8 — Entra ID validation exists
          server-side but MSAL is not wired on the device, so
          `signInWithMicrosoft` cannot obtain a token. Hiding a button that
          cannot work is a defensible product call. It is recorded because the
          app and the handoff now disagree, and that disagreement should be a
          decision someone made rather than a drift nobody noticed.

          Close by either wiring MSAL (PENDING section 8) or amending HANDOFF.
