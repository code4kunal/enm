SEVERITY: low
PERSONA:  any writer @ any site
ORACLE:   design/HANDOFF.md:9 and section 4 ("5 physical registers ... PM
          Schedule Attention"); CLAUDE.md, which also speaks of five registers
REPRO:
  1. POST /entries with register "pm_schedule".
EXPECTED: HANDOFF lists PM Schedule Attention as one of the five registers
          ground staff digitize, with its own field list and indigo colour.
ACTUAL:   400 VALIDATION_ERROR, fields {"register": "retired"} —
          "PM Schedule Attention has been replaced by Inspections — record it
          against the inspection's own checklist instead."
TEST:     qa/api/test_permissions.py::test_the_pm_register_is_retired

The API is deliberate and says so clearly; migration 0007 retires the register.
This is not a bug in the app. It is that the handoff, CLAUDE.md and the spec
for this very suite all still describe five writable registers when there are
four.

Close by amending the handoff. Recorded because a newcomer reading HANDOFF will
build against a register that cannot be written, and because the suite's own
first cut was written against the wrong number.
