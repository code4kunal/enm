SEVERITY: high
PERSONA:  QA_MGR / any manager @ MBMT
ORACLE:   CLAUDE.md — "one MBMT sheet feeds five registers; TYPE OF WORK routes
          it"; the snag report is the depot's monthly import;
          backend/README.md as the authority on the wire format
REPRO:
  1. GET /sites/MBMT/import-profiles — "MBMT monthly snag report",
     target `snagReport`, 19 mappings.
  2. Open Site config -> Import profiles in the client.
EXPECTED: The profile reads as a Snag report import, and a manager can create
          one for a new site.
ACTUAL:   `snagReport` appears nowhere in `app/lib`. The client's
          `ImportTarget` enum held nine values; the server declares eleven.
          `ImportTarget.fromName` falls back to `vehicles` for anything it does
          not recognise, so the profile reads as a **Vehicles** import.

          profile                       server target  client read it as
          MBMT fleet from snag report   vehicles       vehicles
          MBMT monthly snag report      snagReport     vehicles   <- wrong

WORSE THAN A MISSING MENU ENTRY. Saving that profile from the screen sends back
what the client parsed. Demonstrated on the scratch database:

          PUT /sites/MBMT/import-profiles/{id}  -> 200
          'MBMT monthly snag report' is now target=vehicles

A manager opening the profile to change one column heading, and pressing Save,
silently replaces the depot's monthly snag mapping with a vehicles import.
Nothing errors, and the next month's sheet has nowhere correct to go.

STATUS:   CLOSED 2026-08-19.

          `snagReport` added to the client enum with the sheet's own field
          list, mirroring SNAG_FIELDS. `pmSchedule` added too — not to be
          offered, but so a profile written before that register was retired
          still parses as itself; `ImportTarget.selectable` is what the picker
          uses. Adding both made a `switch` in the offline demo store
          non-exhaustive, which the analyzer caught: an enum that grows now
          forces someone to decide what each new target means.

          The silent `orElse: () => vehicles` remains, so the guard is a test
          rather than the type system.

TEST:     qa/api/test_contract_shape.py::
          test_the_client_knows_every_import_target_the_server_can_send

WHY NOTHING CAUGHT IT: `tools/check_contract.py` compares Dart casts against
OpenAPI schema types across 298 assumptions. An enum's *members* are neither a
cast nor a type, so a target the client had never heard of was invisible to it.
Reported by the user, not by the floor — the floor now covers it.
