SEVERITY: high
PERSONA:  QA_MGR (manager) @ QASITE
ORACLE:   backend/README.md "Entry edit / photo: creator, or any
          supervisor/manager on that depot" — editing is a supported action;
          design/HANDOFF.md section 5 ("Edit button ... opens the entry form
          prefilled; save updates in place")
REPRO:
  1. GET /entries?site=...&register=breakdown
  2. PUT /entries/{id} with the `data` object exactly as returned.
EXPECTED: An entry read from the API can be written back unchanged. That is
          what "opens the form prefilled; save updates in place" means: the
          client reads, the user edits one field, the client writes it back.
ACTUAL:   400 VALIDATION_ERROR
          {"resolved_at": "Extra inputs are not permitted"}

          GET serialises `resolved_at` into a breakdown's `data`; `BreakdownData`
          forbids extras and has no such field. The response cannot be used as
          a request.

          register          round-trip
          breakdown         400 — rejected on `resolved_at`
          work_done         200
          coolant           200
          driver_complaint  200

TEST:     qa/api/test_entry_lifecycle.py::test_an_entry_can_be_written_back_unchanged
          backend/tests/test_entries.py::test_a_breakdown_can_be_written_back_unchanged
          backend/tests/test_entries.py::test_a_resolved_breakdown_still_round_trips

STATUS:   CLOSED 2026-08-19. `resolved_at` no longer appears in a breakdown's
          serialised `data`. Lifecycle state stays on the entry, where `status`
          already carries it; the register payload mirrors a paper column and
          the paper has no such column. `csv_details` had been skipping the key
          by hand — the same bug surfacing in a second place — and that
          special case is gone with it. All four registers round-trip.

Breakdown is the only register that fails, and it is the one with an
open/resolved lifecycle, its own tracker screen and the most reason to be
edited. Any client doing read-modify-write on a breakdown must know to strip
one key, and nothing in the contract says so.

Fix by either omitting `resolved_at` from the serialised `data` (it is
lifecycle state, not register data — `status` already carries it) or accepting
and ignoring it on input. The first is cleaner: the paper register has no such
column.
